# 실제 환경변수 값(예: DB 접속 정보, JWT 시크릿)**을 로컬/서버별로 저장하는 비공개 설정 파일.

PORT=4000

JWT_SECRET=개발용_긴_랜덤_문자열으로_바꾸세요

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=davinci_loop

# 선택 사항: 로컬 Ollama 연동 (미설정 시 로컬 fallback 질문 생성 사용)
OLLAMA_URL=http://localhost:11434/api/generate
OLLAMA_MODEL=llama3.1:8b
